import AttiraApp from '@/components/attira/AttiraApp';

// The ATTIRA Skin module — the imported "ATTIRA Skincare v2" design, implemented
// as a self-contained, interactive surface. Kept apart from the ancestralk shell.
export default function AttiraPage() {
  return <AttiraApp />;
}
